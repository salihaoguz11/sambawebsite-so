export type NavigationItem = {
  label: string;
  href: string;
};

export type Service = {
  title: string;
  description: string;
  bullets: string[];
};

export type PortfolioItem = {
  title: string;
  category: string;
  summary: string;
  results: string;
  linkLabel: string;
};
